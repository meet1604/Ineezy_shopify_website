<div class="ineezy-product-card-v1" data-product-id="{{ product.id }}">
  <div class="ineezy-product-card-v1-image-action">
    <a 
      {% if product.available %}
        href="
          {{ product.url }}
          " 
          {% else %}
        {% endif %}
      
      
      style="text-decoration:none">
      <div class="ineezy-product-card-v1-image">
        {% if product.featured_image %}
          <img
            class="ineezy-product-image-one active-image"
            src="{{ product.featured_image | img_url: 'grande' }}"
            alt="{{ product.title | escape }}"
            data-default-image="{{ product.featured_image | img_url: 'grande' }}"
          >
        {% endif %}
        {% if product.images[1] %}
          <img
            class="ineezy-product-image-two"
            src="{{ product.images[1] | img_url: 'grande' }}"
            alt="{{ product.title | escape }}"
          >
        {% endif %}
      </div>
    </a>
    <div class="ineezy-product-card-v1-imae-action-wrapper">
      <div class="ineezy-product-card-v1-action">
        <div class="ineezy-product-card-v1-action-tag-wrapper">
          {% if product.compare_at_price > product.price %}
            <div class="ineezy-product-card-v1-action-tag tag tag-primary">
              Sale
              {{ product.compare_at_price | minus: product.price | times: 100 | divided_by: product.compare_at_price }}%
            </div>
          {% endif %}

          {% if product.available %}
            {% else %}
            <div class="ineezy-product-card-v1-action-tag tag tag-danger">
              Out Of Stock
            </div>
          {% endif %}




          {% if product.tags contains 'new' %}
            <div class="ineezy-product-card-v1-action-tag tag tag-secondary">New</div>
          {% endif %}
        </div>
        <div class="ineezy-product-card-v1-action-icon-wrapper">
          <!-- <button
            class="wishlist-btn ineezy-product-card-v1-action-icon "
            data-product-handle="{{ product.handle }}"
            data-product-price="{{ product.price | money_without_currency }}"
            data-product-image="{{ product.featured_image | img_url: '300x300' }}"
            data-icon-default="{{ 'new-icon-wishlight.svg' | asset_url }}"
            data-icon-filled="{{ 'new-icon-wishlight-fill.svg' | asset_url }}"
          >
            <img class="wishlist-icon" src="{{ 'new-icon-wishlight.svg' | asset_url }}" alt="Add to wishlist">
          </button> -->

          <div class="ineezy-product-card-v1-action-icon wishlist-main">
             {% render 'wishlisthero-collection-product' with product: product %}
          </div>


          {% comment %} <button
            type="button"
            id="quick-view-btn"
            class="ineezy-product-card-v1-action-icon btn-icon-hover-action"
            data-product-handle="{{ product.handle }}"
          >
            <img src="{{ 'new-icon-quick-view.svg' | asset_url }}" alt="Quick View">
          </button> {% endcomment %}
        </div>
      </div>
      <div class="ineezy-product-card-v1-action-button-wrapper d-none">
    


      </div>
    </div>
  </div>
  <div class="ineezy-product-card-v1-content">
    <div class="ineezy-product-title cursor-pointer" onclick="window.location.href='{{ product.url }}'">
      <h6>{{ product.title }}</h6>
      <div class="ineezy-product-price">
        <h5>
          {% if product.price_varies %}
            {{ product.price_min | money }} - {{ product.price_max | money }}
          {% else %}
            {{ product.price | money }}
          {% endif %}
          {% if product.variants.first.compare_at_price > product.variants.first.price %}
              <span class="line-through text-gray-500">
                {{ product.variants.first.compare_at_price | money }}
              </span>
          {% endif %}
        </h5>
      </div>
    </div>
    <div class="ineezy-product-main-button">

            {% comment %} <button
          type="button"
          class="ineezy-product-card-v1-action-button btn  btn-light-primary {% unless product.available %}disabled{% endunless %}"
          onclick="window.location.href='{{ product.url }}'"
          {% unless product.available %}
            disabled
          {% endunless %}
        >
          Buy Now
        </button> {% endcomment %}

      
        <!-- Add to Cart Button -->
        {% if product.variants.size > 1 %}
          <button
            type="button"
            class="ineezy-product-card-v1-action-button btn  btn-light-primary {% unless product.available %}disabled{% endunless %}"
            onclick="window.location.href='{{ product.url }}'"
            {% unless product.available %}
              disabled
            {% endunless %}
          >
            Add to Cart
          </button>
        {% else %}
          <form action="{{ routes.cart_add_url }}" method="post" class="add-to-cart-form w-100">
            <input type="hidden" name="id" value="{{ product.variants.first.id }}">
            <input type="hidden" name="quantity" value="1">
            <button
              type="button"
              class="ineezy-product-card-v1-action-button btn btn-light-primary {% unless product.available %}disabled{% endunless %}"
              onclick="{% if product.available %}addToCartWithDawn('{{ product.variants.first.id }}'){% endif %}"
              {% unless product.available %}
                disabled
              {% endunless %}
            >
              {% if product.available %}
                Add to Cart
              {% else %}
                Out Of Stock
              {% endif %}
            </button>



            
          </form>

        {% endif %}
    </div>

    
    <div class="ineezy-product-variation-metal-wrapper d-none">
      {% if product.options contains 'Color' %}
        <div class="ineezy-product-variation">
          {% for variant in product.variants %}
            {% if variant.available and variant.option1 %}
              <div class="ineezy-product-variation-item">
                <span
                  class="variant-swatch"
                  style="background-color: {{ variant.option1 | downcase }};"
                  title="{{ variant.option1 }}"
                  data-variant-id="{{ variant.id }}"
                  data-image-url="{% if variant.image %}{{ variant.image | img_url: 'grande' }}{% else %}{{ product.featured_image | img_url: 'grande' }}{% endif %}"
                ></span>
              </div>
              <!-- DEBUG: {{ variant.image | img_url: 'grande' }} -->
            {% endif %}
          {% endfor %}
        </div>
      {% endif %}
      <!--
        <div class="ineezy-product-metal-text">
           <p>{{ product.description | strip_html | truncate: 100 }}</p>
        </div>
      -->      
    </div>
  </div>
</div>







